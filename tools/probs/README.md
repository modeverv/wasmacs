# Probs

This directory holds prototype code and one-off probes used to decide build or
implementation direction. Code here is not the product runtime path.

`legacy-root-smokes/` contains the old root-level smoke scripts that predate the
current `tests/` and `tools/scripts/probe-*` layout.
