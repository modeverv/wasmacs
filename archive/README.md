# Archive

Old generated outputs, superseded backups, and files that no longer have a live
runtime or test role can be moved here instead of staying at the project root.

`old-logs/` keeps historical validation and probe evidence from before the
current ignored-logs policy. New logs should be written to `logs/` and left
untracked.
